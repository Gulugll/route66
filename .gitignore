# Binaries
/bin/
*.exe
*.test

# GoLand
.idea/

# Claude local personal settings (per-user overrides, not shared)
.claude/settings.local.json

# Env / secrets
.env
*.key

# Local toolchain caches (sandbox workarounds, not project code)
.gocache/
.gopath/
.gomodcache/

# Locally compiled Redis (Docker unavailable in this env)
.redis-src/
.DS_Store
.npmcache/
